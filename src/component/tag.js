import React, { Component } from 'react'
import styled from 'styled-components'

const tag = styled.span`
    background: #FF334B;
    padding: 0.5rem 1rem;
    color: white;
    font-weight: 500;
    font-size: 1.2rem;
    border-radius: 3px;
`

export default tag
